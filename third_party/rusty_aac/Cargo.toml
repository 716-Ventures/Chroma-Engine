# Chroma: local 0.5.0 source with bounded streaming encoder extension.
[package]
name = "rusty_aac"
version = "0.5.0"
edition = "2021"
rust-version = "1.85"
license = "Apache-2.0"
authors = ["Mata Network <https://www.mata.network>"]
repository = "https://github.com/Remade-With-Rust/remade_ffmpeg_rs"

[features]
default = ["simd"]
simd = []
simd-avx512 = ["simd"]
lab = []

[dev-dependencies]
rusty_alloc-api = "0.3"
